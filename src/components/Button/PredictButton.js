import React from "react";
import { Button } from "@material-ui/core";

const PredictButton = (props) => {
  return (
    <>
      <Button {...props}>PREDICT</Button>
    </>
  );
};

export default PredictButton;
